# Homebrew tap

Casks published by Daniel Reynolds' local signed releases.

Each cask is generated from a public GitHub release and pins the release
archive by SHA-256. Project-specific installation instructions live in the
corresponding source repository.

Casks are macOS-only. On Linux, install from the release tarball or from
the package registry the project documents.
